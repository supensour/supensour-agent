# templates/

Reserved for reusable output scaffolds this skill fills in (spec skeletons, plan formats).
Empty for now — the analyst builds plans inline and the writer emits specs directly.
Conventions live in `../rules/`, process in `../roles/`.
